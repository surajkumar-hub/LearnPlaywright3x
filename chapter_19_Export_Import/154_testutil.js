import { BASE_URL, formatUpperCaseString } from './testutil.js';

console.log(BASE_URL);

let result = formatUpperCaseString("Pramod");
console.log(result);

// console.log(fname); // not available because it has not exported