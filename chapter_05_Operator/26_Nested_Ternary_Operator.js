// Multiple Condition

let age = 26;
// If age > 18 -> he will go to goa, else no
// if age > 25 -> drink else no 
let is_pramod_enjoy = age > 18 ? (age > 26 ? "Drink" : "No") : false;
console.log(`Can pramod Drink? : ${is_pramod_enjoy}`);
